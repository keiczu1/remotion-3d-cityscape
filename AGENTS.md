# Инструкции AGENTS.md для Remotion

## Назначение

Этот репозиторий является рабочей базой для производства роликов формата `ranking corridor` на `Remotion`, `React`, `@remotion/three` и `React Three Fiber`.

Система должна быть воспроизводимой только по файлам репозитория. Не полагайся на память прошлых чатов, скрытое глобальное состояние или устные договоренности.

## Язык общения

- Все ответы, вопросы, планы, пояснения, промежуточные отчеты и итоговые сообщения ИИ в этом репозитории должны быть только на русском языке.
- Исключения допускаются только для кода, команд, путей, URL, API, названий пакетов, ключей конфигурации и других технических сущностей, которые нельзя безопасно переводить.
- Если пользователь явно просит другой язык, можно ответить на нем только в пределах этого запроса.

## Дефолты сессии

- Считай репозиторий продакшн-базой `ranking corridor`, а не песочницей для хаотичного R&D.
- Работа по умолчанию должна укладываться в модель одного репозитория и одного окна IDE.
- Изоляция по умолчанию идет через `projects/<project-slug>/` внутри текущего репозитория.
- Не предлагай `git worktree` и не уводи работу в отдельный репозиторий, если пользователь сам этого не просил.
- Новый код, модули и элементы сцены по умолчанию должны сначала оставаться на уровне конкретного проекта, а не переноситься сразу в библиотеку.
- Перенос в библиотечный слой допустим только после финального утверждения проекта и отдельного аудита библиотеки.
- Канонический формат видео по умолчанию:
  - `1920x1080`
  - `60 fps`
- Не спрашивай про разрешение, FPS и финальный файл, если пользователь явно не хочет другой формат.
- Целевой дефолтный результат: Studio-ready Remotion-композиция, которая корректно открывается и проверяется в Remotion Studio.
- Не рендери `.mp4`, `.mov` или другой финальный файл, если пользователь явно не попросил экспорт.
- Считай короткий запрос вроде `Сделай ролик по теме ...` достаточным стартом для нового проекта.

## Источники правды

Сначала подними этот файл как быстрый repo-session контракт.

После этого открой `docs/README.md` как карту активной документации.

Если нужно понять правила проекта глубже, читай документы в таком порядке:

1. `docs/README.md`
2. `docs/canon/ranking-corridor-format.md`
3. `docs/canon/ranking-corridor-working-mode.md`
4. `docs/canon/remotion-project-rules.md`
5. `projects/README.md`

Роли документов такие:

- `docs/canon/ranking-corridor-format.md` — канон жанра и допустимой вариативности.
- `docs/canon/ranking-corridor-working-mode.md` — рабочий цикл, статусы, `preview-gate` и контракты project-артефактов.
- `docs/canon/remotion-project-rules.md` — технические инварианты Remotion-проекта.
- `projects/README.md` — единственный владелец структуры `projects/<project-slug>/`.
- `docs/templates/` — шаблоны project-артефактов.
- `docs/library/ranking-corridor-module-registry.md` — реестр уже подтвержденных библиотечных модулей.
- `docs/Examples/` — сильные референсы, но не источник правил.

## Обязательные локальные skills

Используй эти repo-local skills, когда они подходят:

- `justdoit` — используй по умолчанию почти для любой нетривиальной задачи в репозитории.
- `ranking-corridor-launch` — используй, когда пользователь приходит с новой темой и нужно дойти только до `launch-card`.
- `ranking-corridor-production` — используй, когда у проекта уже есть `launch-card` или готовый project-container и нужно продолжить рабочий цикл до preview, полной сборки и постфинального аудита.
- `remotion-best-practices` — используй всегда, когда меняется Remotion-композиция, тайминг, 3D-сцена, ассеты или поведение рендера.

## Локальные Remotion-rules

- При работе с локальными файлами из `.agents/skills/remotion-best-practices/` считай их приоритетным источником проектных правил по Remotion.
- Не меняй имена файлов skill/rule без явной необходимости: они используются как стабильные точки входа для локальных инструкций.

## Новый ролик по умолчанию

Для нового запроса по умолчанию используй такой маршрут:

1. Если есть только тема, запускай `ranking-corridor-launch`.
2. Сделай первичную классификацию темы.
3. Задай один пакет действительно нужных вопросов.
4. Точные ограничения пакета вопросов и практическую форму `launch-card` бери из `docs/canon/ranking-corridor-working-mode.md` и `docs/templates/ranking-corridor-launch-card-template.md`.
5. После ответа собери короткую `launch-card`.
6. Не переходи в production-этап на launch-этапе.
7. Если у проекта уже есть `launch-card`, переходи в `ranking-corridor-production`.
8. В production обязательно пройди через `preview-gate` до полной сборки композиции.
9. Полная сборка композиции не означает автоматический экспорт финального медиафайла.
10. Аудит библиотеки запускай только после финального статуса проекта.

## Структура проекта

По умолчанию используй такую схему:

- `projects/<project-slug>/` — project-container конкретного ролика.
- `src/compositions/<project-slug>/` — код композиции конкретного ролика.
- `public/ranking-corridor/<project-slug>/` — локальные публичные ассеты ролика.
- `src/lib/ranking-corridor/` — только для реально подтвержденных reusable-модулей после promotion.

Не переноси код в библиотеку заранее.

## Пользовательский UX

- Держи пользовательский интерфейс простым и разговорным.
- Не перекладывай на пользователя внутреннюю механику workflow, если в этом нет реального блокера.
- Не требуй длинного интервью там, где хватает одного пакета вопросов.
- Не строй всю сцену сразу, если канон требует сначала `preview-gate`.
- Если приходится выбирать между reuse и novelty, проговаривай компромисс явно и кратко.

## Стиль ответов

- Отвечай кратко, по делу и в продакшн-режиме.
- Не дублируй большие куски канона прямо в ответах, если можно сослаться на владеющий документ и следовать ему.
- Если меняешь workflow, канон или библиотечный слой, обновляй связанную документацию вместе с изменением, а не задним числом.
